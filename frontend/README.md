# MatchLab Frontend

MatchLab 校园活动与项目协作平台前端。

```bash
npm install
cp .env.example .env.local
npm run dev
```

常用命令：

```bash
npm test
npm run lint
npm run build
npm run start
```

API 地址通过 `NEXT_PUBLIC_API_BASE_URL` 配置。完整本地开发和 Nginx 部署说明见 [`../docs/FRONTEND.md`](../docs/FRONTEND.md)。
